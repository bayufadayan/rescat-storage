PORT=8080
BASE_URL=http://localhost:8080

# direktori publik yang akan di-serve di /files
UPLOAD_DIR_PUBLIC=public

# daftar bucket yang diizinkan (pisah koma, harus ada foldernya)
ALLOWED_BUCKETS=preview-bounding-box,roi-face-cat,result,original-photo,remove-bg

# ekstensi file yang diizinkan
ALLOWED_EXT=jpg,jpeg,png,webp,pdf

# batas ukuran upload (MB)
MAX_FILE_MB=8

# origin FE yang boleh akses (CORS)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000,http://localhost:8000
